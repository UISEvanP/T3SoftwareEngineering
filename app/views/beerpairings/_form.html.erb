<%= form_with(model: beerpairing, local: true) do |form| %>
  <% if beerpairing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beerpairing.errors.count, "error") %> prohibited this beerpairing from being saved:</h2>

      <ul>
      <% beerpairing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :weight %>
    <%= form.number_field :weight %>
  </div>

  <div class="field">
    <%= form.label :maindish_id %>
    <%= form.select :maindish_id, Maindish.all.collect { |m| [m.name, m.id]} %>
  </div>

  <div class="field">
    <%= form.label :beer_id %>
    <%= form.select :beer_id, Beer.all.collect { |b| [b.name, b.id]}%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
